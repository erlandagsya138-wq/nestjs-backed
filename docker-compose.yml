version: '3.8'

services:
  nestjs-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: durian-nestjs-backend
    restart: always
    ports:
      - "3001:3001"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"